// ClickFunnels Configuration
// This is just a placeholder - actual API calls go through the backend server
// The real configuration is in server.py for security

const CLICKFUNNELS_CONFIG = {
    enabled: true,
    // Backend handles all ClickFunnels API calls
    // No sensitive data exposed in frontend
};
